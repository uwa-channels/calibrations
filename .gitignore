# Library MAT-files: 206 MB, fetched from Zenodo on demand.
*.mat
!artifacts/.gitkeep

# Everything a run produces, except REPORT.md and the figures it embeds --
# those are committed to main, so the README can link to a current report.
artifacts/
__pycache__/
*.pyc
